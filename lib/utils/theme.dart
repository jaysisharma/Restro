import 'package:flutter/material.dart';

// Define the color palette for your app
class AppColors {
  static const Color background = Color(0xFFF6E1B3); // Light warm beige
  static const Color primary = Color.fromARGB(255, 169, 48, 54); // Rich burgundy
  static const Color iconColor = primary; // Icon uses the same burgundy color
  static const Color textColor = primary; // Text also uses the same burgundy color
}
